# geometry_docker_machine - show the current docker machine name

[[ -n $DOCKER_MACHINE_NAME ]] || return
ansi ${GEOMETRY_DOCKER_MACHINE_COLOR:=blue} "(${DOCKER_MACHINE_NAME} ${GEOMETRY_DOCKER_MACHINE_SYMBOL:="󰡨"})"
